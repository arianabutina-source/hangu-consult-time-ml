fastapi==0.138.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
joblib==1.5.3

# xgboost is required again: the API now loads the *entire* model ladder
# (ml/artifacts/models/{classification,regression}/*.joblib) to show every
# model's prediction side by side, not just the deployed RandomForest
# pipeline -- and one ladder entry per task is an XGBClassifier/Regressor.
# Unpickling those .joblib files fails at startup without this installed,
# even though RandomForest remains the single "deployed" model. This does
# reintroduce xgboost's ~300MB CUDA dependency (nvidia-nccl-cu12) into the
# image, which was previously avoided -- an accepted tradeoff for showing
# real XGBoost predictions in the app.
xgboost==3.3.0
