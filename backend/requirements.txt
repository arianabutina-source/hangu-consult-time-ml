fastapi==0.138.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
joblib==1.5.3

# xgboost is intentionally NOT included: the currently serialized pipelines
# (ml/artifacts/*.joblib) use RandomForest, not XGBoost (see metadata.json's
# "best_model"). xgboost's Linux wheel pulls in a ~300MB CUDA dependency
# (nvidia-nccl-cu12) even for CPU-only use, which needlessly triples the
# deployed image size. If a future retrain selects an XGBoost model as best,
# add `xgboost==3.3.0` back here before deploying that version.
